@books.map do |book|
  {
    id: book.id,
    title: book.title,
    author: book.author,
    categories: book.categories,
    available: book.available,
    categories: book.categories, # add number availale to rent later
  }
end
